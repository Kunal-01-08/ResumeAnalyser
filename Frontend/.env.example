# Local development: requests go through Vite's /api proxy.
VITE_BACKEND_URL=/api

# Production: set this in your hosting provider to the public HTTPS URL of
# the FastAPI service, for example:
# VITE_BACKEND_URL=https://api.example.com
